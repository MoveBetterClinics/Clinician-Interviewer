/**
 * Move Better Equine interview context overlay.
 *
 * Stub — returns empty string until equine-specific interview context is
 * authored (sport-horse vs. trail / pleasure / dressage discipline angles,
 * common subtle-signs vocabulary, owner-vs-trainer interview probes, etc.).
 * See `brands/people/interviewContext.js` for the people-paradigm reference
 * implementation.
 *
 * The function name mirrors the people overlay so `src/lib/prompts.js` can
 * import a single symbol regardless of brand.
 */

export function formatPNWContextForPrompt(_condition) {
  return ''
}
