/**
 * Move Better Animal Chiropractic interview context overlay.
 *
 * Stub — returns empty string until animals-specific interview context is
 * authored (working- vs. companion-dog scenarios, senior-pet stiffness
 * vocabulary, owner-of-cat vs. owner-of-dog interview angles, etc.).
 * See `brands/people/interviewContext.js` for the people-paradigm reference
 * implementation.
 *
 * The function name mirrors the people overlay so `src/lib/prompts.js` can
 * import a single symbol regardless of brand.
 */

export function formatPNWContextForPrompt(_condition) {
  return ''
}
